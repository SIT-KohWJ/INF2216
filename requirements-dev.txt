# Development & test dependencies (not installed in the production image).
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.0            # test runner (tests/)
