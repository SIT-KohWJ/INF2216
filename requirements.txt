# Web framework + WSGI server
Flask==3.1.3
gunicorn==23.0.0

# Database & ORM (PostgreSQL in docker/CI; SQLite fallback for bare local runs)
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# Auth / session
Flask-Login==0.6.3
Flask-WTF==1.2.2          # CSRF protection (B5)
WTForms==3.1.2
email-validator==2.2.0   # required by WTForms Email() on login/register forms
Flask-Limiter==3.8.0      # per-IP rate limiting (C1, C2)

# Crypto: AES-256-GCM field/file encryption (A3), HMAC anonymity (A2),
# ECDSA audit-log signing. pycryptodome is the lib the services import.
bcrypt==4.2.1
pycryptodome==3.21.0     # bumped from 3.18.0 (clears stale pip-audit advisories)

# Input safety / file validation
bleach==6.2.0            # XSS sanitisation (B2)
python-magic==0.4.27     # magic-byte MIME validation (B3, B7)

# Local dev convenience
python-dotenv==1.2.2
